pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
    }
    stages {
        stage("Build") {
            steps {
                sh "mvn clean install"
            }
        }
    }
}
