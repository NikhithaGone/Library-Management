pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
    }
    stages {
        stage("Build") {
            steps {
                bat "mvn clean install"
            }
        }
    }
}
